import { ApisSection } from "@/components/apis-section";

export default function ApisPage() {
  return (
    <main className="h-full flex items-center justify-center bg-muted/30">
      <ApisSection />
    </main>
  );
}
