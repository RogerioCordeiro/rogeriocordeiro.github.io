import { SitesSection } from "@/components/sites-section";

export default function SitesPage() {
  return (
    <main className="h-full flex items-center justify-center bg-gradient-to-br from-muted/30 to-background">
      <SitesSection />
    </main>
  );
}
